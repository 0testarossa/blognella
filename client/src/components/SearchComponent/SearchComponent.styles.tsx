import styled from 'styled-components';

export const StyledSearch = styled.div`
    label{
        color: white;
    }
    .MuiInputBase-input{
        color: #dfdfdf;
    }
    button{
        height: 30px;
        margin-top: 25px;
    }
    
    display: flex;
    flex-direction: row;
`
import styled from 'styled-components';

export const StyledSearch = styled.div`
    label{
        color: white;
    }
    .MuiInputBase-input{
        color: #dfdfdf;
    }
    button{
        height: 30%;
    }
    
    display: flex;
    flex-direction: row;
    align-items: center;
`